label size
    pop size.lookAt
    sizeof size.varSize,%size.lookAt
    push %size.varSize
    free %size.varSize
    ret

label println
    pop println.stringToPrint
    out %println.stringToPrint
    out \n
    ret