version format:
a.b.c.d

a (required)) The release version
b (required)) The major version
c (optional)) The minor version
d (optional)) Patch number

Versions and Notes:
1.0 (working on) "The basics"
    While Loop (done)
    if else
    data types (done)

1.2 (future) "Functions and scopes update"
    includes
    Basic functions
    arrays
    recursion (change to runtime with func calls)
    else if
    free keyword (free a variable from memory)
    force keyword (force set a flag)

1.3 (future) "Operations Expanded"
    bitwise operations
    power operator
    modulus operator
    dynamic type casting
    getting type of a variable